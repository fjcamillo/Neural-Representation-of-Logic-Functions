import numpy as np
from functools import reduce

def perceptron(weight, bias, x):
    logit = np.sum(np.add(np.dot(x, weight), bias))
    return logit

def main():
    AND = {
        'w1': ,
        'w2': ,
    }
    
